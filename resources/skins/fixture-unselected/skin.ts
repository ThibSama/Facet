import './skin.css';

/**
 * fixture-unselected — test fixture entrypoint.
 *
 * Deliberately inert. Its only job is to exist in the build manifest while
 * being absent from every rendered document. See skin.css for the rationale.
 */
const FIXTURE_SKIN_ID = 'fixture-unselected';

export { FIXTURE_SKIN_ID };
